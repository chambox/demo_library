from setuptools import setup, find_packages

setup(
    name="demo_library",  # Replace with your package's name
    version="0.1.0",  # Package version
    packages=find_packages(),  # This will find and include all packages automatically
)
